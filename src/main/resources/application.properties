spring.application.name=api-ladxidoar
spring.datasource.url=jdbc:postgresql://${DB_HOST:localhost}:${DB_PORT:5432}/${DB_NAME:db_ladxidoar}
#spring.datasource.url=jdbc:postgresql://${DB_HOST}:${DB_PORT}/${DB_NAME}?sslmode=require
spring.datasource.username=${DB_USER:postgres}
spring.datasource.password=${DB_PASSWORD:C@y3tanorojaS}
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.main.allow-circular-references=true

# Cloudinary Configuration
cloudinary.cloud-name=djflw4po4
cloudinary.api-key=382647193924132
cloudinary.api-secret=_Z48OoZ6TfyP6Da9Yh1mkP3a_v8

# Para ver errores detallados
server.error.include-message=always
server.error.include-binding-errors=always

# Tamaños de archivo (ajusta según necesites)
spring.servlet.multipart.max-file-size=10MB
spring.servlet.multipart.max-request-size=10MB

# Nivel de logs para Hibernate y JPA
logging.level.org.hibernate.SQL=DEBUG
logging.level.org.hibernate.type.descriptor.sql.BasicBinder=TRACE
logging.level.org.springframework.data.jpa.repository=DEBUG
logging.level.org.springframework.transaction=TRACE

# Para ver transacciones
logging.level.org.springframework.orm.jpa=DEBUG
logging.level.org.springframework.transaction.interceptor=TRACE

spring.jpa.properties.hibernate.query.in_clause_parameter_padding=true
spring.jpa.properties.hibernate.format_sql=true